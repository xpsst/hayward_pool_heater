esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}

logger:

external_components:
  - source:
      type: local
      path: ../../../components
    components: [hwp]

climate:
  - platform: hwp
    id: pool_heater
    name: "Pool Heater"
    pin_txrx: ${pin_txrx}
    input:
      xps100_r11_max_heating_setpoint:
        name: "XPS-100 Max Heating Setpoint"
